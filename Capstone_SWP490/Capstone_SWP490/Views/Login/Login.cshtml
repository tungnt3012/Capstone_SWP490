@model Capstone_SWP490.app_user

@{
    Layout = "~/Views/Shared/_SigninLayout.cshtml";
    ViewBag.Title = "Login";
}

<div id="wrapper-home">
    <div class="back">
        <div class="div-center-chang">
            <div class="content">
                <div class="form-group">
                    <label for="exampleInputUserLogin" id="homeUserChangPassword">OLP ICPC Login Form</label>
                    </div>
                    @using (Html.BeginForm("Login", "Login"))
                    {
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.user_name, new { @class = "form-control", id = "exampleInputLoginUser", placeholder = "Username" })
                        </div>
                        <div class="form-group">
                            @Html.PasswordFor(x => x.psw, new { @class = "form-control", id = "exampleInputLoginPassword", placeholder = "Password" })
                        </div>
                        <div class="form-group">
                            <p style="color:red">
                                @ViewData["LoginError"]
                            </p>
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-primary" id="btnHomeLogin">Login</button>
                        </div>
                        <div class="form-group">
                            <div class="pull-left">
                                @Html.ActionLink("Back to home", "", "Home")
                            </div>
                            <div class="pull-right">
                                @Html.ActionLink("Forget your password", "ForgotPassword", "Login")
                            </div>
                        </div>
                    }
                </div>
            </div>
    </div>
</div>
